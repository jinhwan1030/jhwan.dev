services:
  homepage:
    image: legyeseul/jhwan-homepage:latest
    container_name: jhwan-homepage
    restart: unless-stopped
    ports:
      - "4321:80"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1/blog/ >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
